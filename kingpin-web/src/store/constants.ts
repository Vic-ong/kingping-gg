export const baseState = {
  loading: false,
  error: false,
};

export const errorState = {
  loading: false,
  error: true,
};

export const loadingState = {
  loading: true,
  error: false,
};

export default baseState;
